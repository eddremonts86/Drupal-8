<?php

/**
 * @file
 * Contains rp_competition.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function rp_competition_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the rp_competition module.
    case 'help.page.rp_competition':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Rp Competition Entity') . '</p>';
      return $output;

    default:
  }
}
