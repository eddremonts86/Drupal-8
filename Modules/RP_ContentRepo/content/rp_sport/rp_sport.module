<?php

/**
 * @file
 * Contains rp_sport.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function rp_sport_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the rp_sport module.
    case 'help.page.rp_sport':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('RP Sport Entity') . '</p>';
      return $output;

    default:
  }
}
