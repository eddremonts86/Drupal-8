<section class='o-frontpage-partners'>
    <header>{{ "Vores Samarbejdspartnere" | t }}</header>
    <ul>
        {% for partner in tags %}
            <li>
                <a href={{ partner.url }}>
                    <img src='{{ file_url(partner.logo) }}' alt="{{ partner.name }}"/>
                </a>
            </li>
        {% endfor %}
    </ul>
</section>